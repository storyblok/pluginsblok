name: CI

on:
  push:
    branches: [main]
    paths:
      - "packages/field-plugin/**"
  pull_request:
    branches: [main]
    paths:
      - "packages/field-plugin/**"

env:
  HUSKY: 0
  NX_REJECT_UNKNOWN_LOCAL_CACHE: 0

jobs:
  build:
    environment: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node

      - name: Check
        run: pnpm nx run-many --target=build,lint,test,test:types --parallel=3 -p="field-plugin-*-template"
